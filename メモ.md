## 1. Data Exploration

SPIDERデータセットの構造を確認する。

- あなたがこの章で理解すべきことはこれだけです。
  - MRI は 3D データとして保存されている
  - マスクも 3D で、各画素にラベル番号が入っている
  - そのままでは学習しづらいので後で整形する

1. Data Exploration
   ここは「まずデータを知る」章です。いきなり学習しません。

やっていることは主にこれです。

画像ファイルとマスクファイルが何個あるか確認する
T1, T2, T2 SPACE のどれがあるかを見る
MHA ファイルの shape や spacing を確認する
マスクにどんなラベル値が入っているか調べる
実際のスライス画像を表示して目で確認する
この章の目的は、「データの正体を知らないまま処理しない」ことです。
機械学習では、ここを飛ばすと後でかなり詰まりやすいです。

あなたがこの章で理解すべきことはこれだけです。

MRI は 3D データとして保存されている
マスクも 3D で、各画素にラベル番号が入っている
そのままでは学習しづらいので後で整形する



元データ:
3D MRI画像
3D 正解マスク

前処理後:
2D MRIスライス
2D 正解マスクスライス


2D MRIスライス
  ↓
モデルに入れる
  ↓
予測マスクが出る
  ↓
正解マスクと比べる
  ↓
Dice / IoU などの指標を測る








## 質問

マスクってなんですか？
マスクとは、画像のどの場所がどの構造かを示した正解ラベル画像のこと。

MRI画像そのものは黒と白というか、濃淡のある普通の医療用画像だけど
マスクは、この画素
つまりマスクがあることで、普通の2DのMRI画像から色々して加工するわけだが
最終的な答えが色付きでわかる。
何が答えかというと、
椎体は赤
脊柱管は緑
椎間板は青
など。

(codex)
マスクとは、**MRI画像の各画素がどの構造に属するかを番号で持っている正解画像**です。

表示するときに、

- 椎体を赤
- 脊柱管を緑
- 椎間板を青

のように色を付けて見せますが、**マスクの中身そのものが最初から赤・緑・青の画像とは限りません**。実体はだいたいこういう番号の画像です。

```text
0 = 背景
1 = 椎体
2 = 脊柱管
3 = 椎間板
```

なので、より正確に言うならこうです。

> MRI画像は濃淡画像。  
> マスクは、そのMRI画像の各画素に「ここは背景」「ここは椎体」「ここは脊柱管」「ここは椎間板」という正解ラベルを付けた画像。  
> 人間が見やすいように表示するとき、ラベル番号を赤・緑・青などの色に変換して重ねて表示する。

「最終的な答えが色付きでわかる」という理解は、**可視化としては正しい**です。  
ただし、学習で使う答えは色ではなく、画素ごとのラベル番号です。
